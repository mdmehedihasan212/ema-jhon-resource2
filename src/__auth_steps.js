/**
 * create firebase account
 * npm i firebase
 * create a file in src firebase init and add into copy initialize Firebase.
 * create auth export for import globally.
 * go firebase setting > authentication and create user sign methods.
 */